apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ik-jenkins.fullname" . }}-test-connection"
  labels:
    {{- include "ik-jenkins.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ik-jenkins.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
