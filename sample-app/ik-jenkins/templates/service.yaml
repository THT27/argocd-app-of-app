apiVersion: v1
kind: Service
metadata:
  name: {{ include "ik-jenkins.fullname" . }}
  labels:
    {{- include "ik-jenkins.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ik-jenkins.selectorLabels" . | nindent 4 }}
